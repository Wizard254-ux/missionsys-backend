services:
  - type: web
    name: missionsys-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Add any other environment variables your app needs below
    # - key: DATABASE_URL
    #   value: your-database-url